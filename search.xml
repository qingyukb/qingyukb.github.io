<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>hexo利用hexo-abbrlink插件生成永久链接</title>
    <url>/undefined/4512133.html</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><ul>
<li>Hexo默认的静态URL格式是 <code>:year/:month/:day/:title</code> ，即按年、月、日、标题的形式生成固定链接<ul>
<li>例如： <code>http://www.xxx.com/2022/11/08/文章标题</code></li>
<li>默认URL的格式会产生很多斜杠，百度蜘蛛在抓取网页时会根据网页权重抓取网页，比如最先抓取的首页（一般都是自己的域名，比较简短），因此在链接里不宜出现过多的斜杠，最好不要超过两个</li>
<li>还有一个问题：如果文章的标题使用了中文，经过转码后，URL会变得特别长，在最后我会贴上对比图。</li>
</ul>
</li>
<li>使用 <code>hexo-abbrlink</code> 插件便可解决以上出现的问题，以往修改标题会导致原始链接失效，使用这个插件后只要不更改文章中的abbrlink值，文章的链接就会是持久的</li>
</ul>
<hr>
<h2 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h2><h3 id="1、安装插件"><a href="#1、安装插件" class="headerlink" title="1、安装插件"></a>1、安装插件</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">javanpm install hexo-abbrlink --save</span><br></pre></td></tr></table></figure>
<blockquote>
<p>具体插件链接： <a href="https://github.com/Rozbo/hexo-abbrlink">hexo-abbrlink</a></p>
</blockquote>
<h3 id="2、配置"><a href="#2、配置" class="headerlink" title="2、配置"></a>2、配置</h3><p>修改 <code>_config.yml</code> 配置文件</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java## permalink: :year/:month/:day/:title/</span><br><span class="line"></span><br><span class="line">permalink: posts/:abbrlink.html  ## 或 :/abbrlink/ （此处可自定义<span class="string">&#x27;posts&#x27;</span>处，如：posts:year/:abbrlink.html 或 :abbrlink.html）</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意：如果使用”:/abbrlink/“，abbrlink后须带”/“，否则点击链接不会跳转到文章页、而是会触发浏览器下载页面代码操作或跳转到空白链接页面</p>
</blockquote>
<p>在 <code>_config.yml</code> 配置文件下增加以下配置<br>如果你是 <code>butterfly</code> 主题，应该都有自己创建了一个主题配置的yml（我的是 <code>_config.butterfly.yml</code> ），以下的配置放在自定义的主题配置的yml文件中也是可以的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java## abbrlink config</span><br><span class="line"></span><br><span class="line">abbrlink:</span><br><span class="line"></span><br><span class="line">  alg: crc32      #support <span class="title function_">crc16</span><span class="params">(<span class="keyword">default</span>)</span> and crc32 进制</span><br><span class="line"></span><br><span class="line">  rep: hex        #support <span class="title function_">dec</span><span class="params">(<span class="keyword">default</span>)</span> and hex  算法</span><br><span class="line"></span><br><span class="line">  drafts: <span class="literal">false</span>   #(<span class="literal">true</span>)Process draft,(<span class="literal">false</span>)Do not process draft. <span class="literal">false</span>(<span class="keyword">default</span>) </span><br><span class="line"></span><br><span class="line">  ## Generate categories from directory-tree</span><br><span class="line"></span><br><span class="line">  ## depth: the max_depth of directory-tree you want to generate, should &gt; <span class="number">0</span></span><br><span class="line"></span><br><span class="line">  auto_category:</span><br><span class="line"></span><br><span class="line">     enable: <span class="literal">true</span>  #<span class="literal">true</span>(<span class="keyword">default</span>)</span><br><span class="line"></span><br><span class="line">     depth:        #<span class="number">3</span>(<span class="keyword">default</span>)</span><br><span class="line"></span><br><span class="line">     over_write: <span class="literal">false</span> </span><br><span class="line"></span><br><span class="line">  auto_title: <span class="literal">false</span> #enable auto title, it can auto fill the title by path</span><br><span class="line"></span><br><span class="line">  auto_date: <span class="literal">false</span> #enable auto date, it can auto fill the date by time today</span><br><span class="line"></span><br><span class="line">  force: <span class="literal">false</span> #enable force mode,in <span class="built_in">this</span> mode, the plugin will ignore the cache, and calc the abbrlink <span class="keyword">for</span> every post even it already had abbrlink.</span><br></pre></td></tr></table></figure>
<ul>
<li><code>alg</code>: 算法(目前支持 <code>crc16</code> 和 <code>crc32</code> 算法，默认值是 <code>crc16</code> )</li>
<li><code>rep</code>: 形式(生成的链接可以是十六进制格式也可以是十进制格式，默认值是十进制格式)</li>
</ul>
<h3 id="3、示例"><a href="#3、示例" class="headerlink" title="3、示例"></a>3、示例</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">htmlcrc16 &amp; hex</span><br><span class="line"></span><br><span class="line">https://test.com/posts/55c6.html</span><br><span class="line"></span><br><span class="line">crc16 &amp; dec</span><br><span class="line"></span><br><span class="line">https://test.com/posts/43212.html</span><br><span class="line"></span><br><span class="line">crc32 &amp; hex</span><br><span class="line"></span><br><span class="line">https://test.com/posts/6ec16a2c.html</span><br><span class="line"></span><br><span class="line">crc32 &amp; dec</span><br><span class="line"></span><br><span class="line">https://test.com/posts/1521457752.html</span><br></pre></td></tr></table></figure>
<blockquote>
<p>更多配置可查看具体插件链接： <a href="https://github.com/Rozbo/hexo-abbrlink">hexo-abbrlink</a></p>
</blockquote>
<hr>
<h2 id="效果展示"><a href="#效果展示" class="headerlink" title="效果展示"></a>效果展示</h2><p>使用插件生成的URL</p>
<p><a href=""><img src="/img/lianjie.png" alt="pic_2"></a></p>
<p>插件生成的URL会更简洁明了</p>
]]></content>
      <categories>
        <category>分享</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>markdown的基本语法</title>
    <url>/undefined/8331de3d.html</url>
    <content><![CDATA[<h2 id="Markdown-基本语法"><a href="#Markdown-基本语法" class="headerlink" title="Markdown 基本语法"></a>Markdown 基本语法</h2><p>以下是 Markdown 的核心语法元素，它们在所有 Markdown 应用中都应该被支持。</p>
<h3 id="标题-Headings"><a href="#标题-Headings" class="headerlink" title="标题 (Headings)"></a>标题 (Headings)</h3><p>Markdown 语法中支持六级标题，通过在行首添加 <strong>#</strong> 符号来定义标题级别， <strong>#</strong> 的数量对应标题的级别。</p>
<ul>
<li><code># 一级标题</code> 渲染为 <code>&lt;h1&gt;一级标题&lt;/h1&gt;</code></li>
<li><code>## 二级标题</code> 渲染为 <code>&lt;h2&gt;二级标题&lt;/h2&gt;</code></li>
<li><code>### 三级标题</code> 渲染为 <code>&lt;h3&gt;三级标题&lt;/h3&gt;</code></li>
<li><code>#### 四级标题</code> 渲染为 <code>&lt;h4&gt;四级标题&lt;/h4&gt;</code></li>
<li><code>##### 五级标题</code> 渲染为 <code>&lt;h5&gt;五级标题&lt;/h5&gt;</code></li>
<li><code>###### 六级标题</code> 渲染为 <code>&lt;h6&gt;六级标题&lt;/h6&gt;</code></li>
</ul>
<p>另外，还可以使用 <strong>\=</strong> 和 <strong>-</strong> 来创建一级和二级标题：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">一级标题</span><br><span class="line">========</span><br><span class="line"></span><br><span class="line">二级标题</span><br><span class="line">--------</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：</p>
<ul>
<li>在 <strong>#</strong> 符号和标题文本之间添加一个空格。</li>
<li>为了兼容性，在标题前后添加空行。</li>
</ul>
<h3 id="段落-Paragraphs"><a href="#段落-Paragraphs" class="headerlink" title="段落 (Paragraphs)"></a>段落 (Paragraphs)</h3><p>Markdown 语法中的段落由一个或多个文本行组成， <strong>使用空行来分隔不同的段落</strong> 。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">这是第一段。</span><br><span class="line"></span><br><span class="line">这是第二段。</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ： <strong>不要使用空格或制表符缩进段落</strong> ，除非段落位于列表中。</p>
<h3 id="换行-Line-Breaks"><a href="#换行-Line-Breaks" class="headerlink" title="换行 (Line Breaks)"></a>换行 (Line Breaks)</h3><p>Markdown 语法中，在一行的末尾添加 <strong>两个或多个空格</strong> ，然后键入回车来创建一个换行。你也可以使用 <code>&lt;br&gt;</code> HTML 标签来换行。</p>
<p><strong>最佳实践:</strong> 为了兼容性，建议使用尾随空格或 <code>&lt;br&gt;</code> 标签。</p>
<h3 id="粗体-Bold"><a href="#粗体-Bold" class="headerlink" title="粗体 (Bold)"></a>粗体 (Bold)</h3><p>Markdown 语法支持两种强调方式：使用 <strong>两个星号 (</strong> ) 或 <strong>两个下划线 (__)</strong> 将文本括起来。</p>
<p><code>**粗体文本**</code> 或 <code>__粗体文本__</code> 渲染为 <code>&lt;strong&gt;粗体文本&lt;/strong&gt;</code> 。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">**这是粗体文本**</span><br><span class="line">__这也是粗体文本__</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：为了兼容性，使用星号来加粗单词中间部分。 例如: <code>Love**is**bold</code> ，同时，建议在加粗的中文文本的前后添加空格。</p>
<h3 id="斜体-Italic"><a href="#斜体-Italic" class="headerlink" title="斜体 (Italic)"></a>斜体 (Italic)</h3><p>Markdown 语法使用 <strong>一个星号</strong> <code>*</code> 或 <strong>一个下划线</strong> <code>_</code> 包裹需要斜体的文本。</p>
<p><code>*斜体文本*</code> 或 <code>_斜体文本_</code> 渲染为 <code>&lt;em&gt;斜体文本&lt;/em&gt;</code> 。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="emphasis">_这是斜体文本_</span></span><br><span class="line"><span class="emphasis">_这也是斜体文本_</span></span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：为了兼容性，建议在单词中间使用星号 <code>*</code> 来倾斜。 例如： <code>A*cat*meow</code></p>
<h3 id="粗体和斜体-Bold-and-Italic"><a href="#粗体和斜体-Bold-and-Italic" class="headerlink" title="粗体和斜体 (Bold and Italic)"></a>粗体和斜体 (Bold and Italic)</h3><p>Markdown 语法使用 <strong>三个星号</strong> <code>***</code> 或 <strong>三个下划线</strong> <code>___</code> 包裹需要同时加粗和斜体的文本。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">***这是粗斜体文本***</span><br><span class="line">___这也是粗斜体文本___</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践:</strong></p>
<ul>
<li>为了兼容性，使用星号来斜体加粗单词中间部分。</li>
<li>使用 <code>**_粗斜体_**</code> 或 <code>_**粗斜体**_</code> 更加直观。</li>
</ul>
<h3 id="引用-Blockquotes"><a href="#引用-Blockquotes" class="headerlink" title="引用 (Blockquotes)"></a>引用 (Blockquotes)</h3><p>在段落前添加 <strong>></strong> 符号来创建引用。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="quote">&gt; 这是引用的内容</span></span><br><span class="line"><span class="quote">&gt;</span></span><br><span class="line"><span class="quote">&gt; 这是引用的内容</span></span><br><span class="line"><span class="quote">&gt;</span></span><br><span class="line"><span class="quote">&gt; 这是引用的内容</span></span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：在引用前后各留一个空行，以确保正确渲染。</p>
<p>引用可以包含其他 Markdown 格式的元素进行 <strong>嵌套引用</strong> ，使用多个 <code>&gt;</code> 符号来创建嵌套引用</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="quote">&gt; 这是第一层引用</span></span><br><span class="line"></span><br><span class="line"><span class="quote">&gt; &gt; 这是第二层引用</span></span><br><span class="line"></span><br><span class="line"><span class="quote">&gt; &gt; &gt; 这是第三层引用</span></span><br></pre></td></tr></table></figure>
<h3 id="列表-Lists"><a href="#列表-Lists" class="headerlink" title="列表 (Lists)"></a>列表 (Lists)</h3><p>Markdown 语法支持有序列表和无序列表。</p>
<h4 id="有序列表-Ordered-Lists"><a href="#有序列表-Ordered-Lists" class="headerlink" title="有序列表 (Ordered Lists)"></a>有序列表 (Ordered Lists)</h4><p>使用 <strong>数字加英文句点</strong><code>.</code> 来创建有序列表。 数字不必按顺序排列，但列表应该以数字 <strong>1</strong> 开始。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">1.</span> 第一项</span><br><span class="line"><span class="bullet">2.</span> 第二项</span><br><span class="line"><span class="bullet">3.</span> 第三项</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：为了兼容性，列表项分隔符建议只使用英文句点 <code>.</code>。</p>
<h4 id="无序列表-Unordered-Lists"><a href="#无序列表-Unordered-Lists" class="headerlink" title="无序列表 (Unordered Lists)"></a>无序列表 (Unordered Lists)</h4><p>使用 <strong>减号</strong> <code>-</code> 、 <strong>星号</strong> <code>*</code> 或 <strong>加号</strong> <code>+</code> 来创建无序列表。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">- 第一项</span><br><span class="line"></span><br><span class="line">* 第二项</span><br><span class="line"></span><br><span class="line">+ 第三项</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：</p>
<ul>
<li>在同一个列表中，不要混合使用不同的分隔符。</li>
<li>要使无序列表的条目以数字开头，需要使用反斜杠 ( <code>\</code> ) 进行转义。 例如： <code>- 1968\. A great year!</code></li>
</ul>
<h4 id="嵌套列表-Nested-Lists"><a href="#嵌套列表-Nested-Lists" class="headerlink" title="嵌套列表 (Nested Lists)"></a>嵌套列表 (Nested Lists)</h4><p>可以通过缩进列表项来创建嵌套列表。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">1.</span> 第一项</span><br><span class="line"><span class="bullet">   1.</span> 嵌套第一项</span><br><span class="line"><span class="bullet">   2.</span> 嵌套第二项</span><br><span class="line"><span class="bullet">2.</span> 第二项</span><br></pre></td></tr></table></figure>
<h4 id="在列表中添加其他元素"><a href="#在列表中添加其他元素" class="headerlink" title="在列表中添加其他元素"></a>在列表中添加其他元素</h4><p>要在列表中添加其他元素（如段落、引用或代码块），需要将这些元素缩进。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">-</span> 这是第一项。</span><br><span class="line"><span class="bullet">-</span> 这是第二项。</span><br><span class="line">  这是第二项下的一个段落。</span><br><span class="line"><span class="bullet">-</span> 这是第三项。</span><br></pre></td></tr></table></figure>
<h3 id="代码-Code"><a href="#代码-Code" class="headerlink" title="代码 (Code)"></a>代码 (Code)</h3><p>Markdown 语法中可以使用两种方式来标记代码： <strong>行内代码</strong> 和 <strong>代码块</strong> 。</p>
<h4 id="行内代码-Code-Spans"><a href="#行内代码-Code-Spans" class="headerlink" title="行内代码 (Code Spans)"></a>行内代码 (Code Spans)</h4><p>使用 <strong>反引号</strong> <code>` </code> 包裹行内代码。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">\<span class="code">`这是行内代码\`</span></span><br></pre></td></tr></table></figure>
<p>如果代码本身包含反引号，可以使用 <strong>双反引号 (``)</strong> 来包裹。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">\<span class="code">`\`</span> 这是包含 \<span class="code">` 反引号的代码 \`</span>\`</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong>:</p>
<ul>
<li>注意 <code>`` </code> 首尾的空格</li>
<li><strong>行内代码中的反斜杠不会被转义</strong> 例如： <code>\` </code> 不会被渲染成 <code>` </code></li>
</ul>
<h4 id="代码块-Code-Blocks"><a href="#代码块-Code-Blocks" class="headerlink" title="代码块 (Code Blocks)"></a>代码块 (Code Blocks)</h4><p>可以使用两种 Markdown 语法来创建代码块：缩进和围栏式。</p>
<p><strong>缩进式代码块</strong> ：将代码块的每一行缩进 <strong>至少四个空格</strong> 或一个制表符。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span></span><br><span class="line"><span class="code">    &lt;head&gt;</span></span><br><span class="line"><span class="code">    &lt;/head&gt;</span></span><br><span class="line"><span class="code">    &lt;/html&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>围栏式代码块</strong> ：使用 <strong>三个反引号</strong> <code>``` </code> 或 <strong>三个波浪号</strong> <code>~~~</code> 将代码块包裹起来，可以在开头的反引号或波浪号后添加代码语言的名称，以进行语法高亮。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">\<span class="code">`\`</span>\`python</span><br><span class="line">def hello():</span><br><span class="line"><span class="code">    print(&quot;Hello, world!&quot;)</span></span><br><span class="line"><span class="code">\`\`\`</span></span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">~~~html</span><br><span class="line">&lt;html&gt;</span><br><span class="line">  &lt;head&gt;</span><br><span class="line">  &lt;/head&gt;</span><br><span class="line">&lt;/html&gt;</span><br><span class="line">~~~</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：围栏式代码块更易于使用和阅读，并且支持语法高亮。</p>
<h3 id="分割线-Horizontal-Rules"><a href="#分割线-Horizontal-Rules" class="headerlink" title="分割线 (Horizontal Rules)"></a>分割线 (Horizontal Rules)</h3><p>Markdown 语法使用 <strong>三个或更多星号</strong> <code>***</code> 、 <strong>减号</strong> <code>---</code> 或 <strong>下划线</strong> <code>___</code> 来创建水平分割线。</p>
<p><code>***</code> 、 <code>---</code> 或 <code>___</code> 渲染为 <code>&lt;hr /&gt;</code> 。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line"></span><br><span class="line">***</span><br><span class="line"></span><br><span class="line">+++</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践</strong> ：在水平分割线前后各留一个空行。</p>
<h3 id="链接-Links"><a href="#链接-Links" class="headerlink" title="链接 (Links)"></a>链接 (Links)</h3><p>Markdown 语法中如何添加 URL 链接？</p>
<h4 id="行内链接-Inline-Links"><a href="#行内链接-Inline-Links" class="headerlink" title="行内链接 (Inline Links)"></a>行内链接 (Inline Links)</h4><p>使用 <strong>方括号</strong> <code>[]</code> 包裹链接文本，紧接着使用 <strong>圆括号</strong> <code>()</code> 包裹 URL 链接地址。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">链接文本</span>](<span class="link">https://www.example.com</span>)</span><br></pre></td></tr></table></figure>
<p>还可以在 URL 后面的圆括号中添加 <strong>链接标题</strong> 。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">链接文本</span>](<span class="link">https://www.example.com &quot;链接标题&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>可以使用 <strong>尖括号</strong> <code>&lt;&gt;</code> 来快速将 URL 或邮箱地址转换为链接。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml">&lt;https://www.example.com&gt;</span></span><br><span class="line"><span class="language-xml">&lt;test@example.com&gt;</span></span><br></pre></td></tr></table></figure>
<h4 id="格式化链接"><a href="#格式化链接" class="headerlink" title="格式化链接"></a>格式化链接</h4><p>使用星号 <code>*</code> 可以强调链接: <code>**[链接](url)**</code>, <code>*[链接](url)*</code></p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="strong">**[<span class="string">链接文本</span>](<span class="link">https://www.example.com</span>)**</span></span><br><span class="line"><span class="emphasis">*[<span class="string">*链接文本*</span>](<span class="link">https://www.example.com</span>)*</span></span><br></pre></td></tr></table></figure>
<p>使用反引号将链接表示为代码： <code>[`code`](url)</code></p>
<h4 id="引用式链接-Reference-Links"><a href="#引用式链接-Reference-Links" class="headerlink" title="引用式链接 (Reference Links)"></a>引用式链接 (Reference Links)</h4><p>引用链接将链接的定义与文本分开，提高了 Markdown 的可读性。</p>
<p>引用式链接分为两部分： <strong>链接文本</strong> 和 <strong>链接定义</strong> 。链接文本在文中直接使用，链接定义则放在文档的其他地方。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">链接文本</span>][<span class="symbol">标签</span>]</span><br><span class="line"></span><br><span class="line">[<span class="symbol">标签</span>]: <span class="link">https://www.example.com &quot;链接标题&quot;</span></span><br></pre></td></tr></table></figure>
<p>链接文本使用两个方括号 <code>[链接文本][标签]</code> 。</p>
<p>链接定义可以出现在文档的任何地方，其格式是: <code>[标签]: URL &quot;链接标题&quot;</code></p>
<p>标签不区分大小写。链接的定义部分可以放在文章结尾或段落后方。</p>
<p><strong>最佳实践</strong> ：</p>
<ul>
<li>URL 中如果包含空格，建议使用 <code>%20</code> 编码空格，或者使用 <code>&lt;a&gt;</code> 标签。</li>
<li>URL 中如果包含括号，建议分别使用 <code>%28</code> 和 <code>%29</code> 来编码左右括号，或使用 <code>&lt;a&gt;</code> 标签。</li>
</ul>
<h3 id="图片-Images"><a href="#图片-Images" class="headerlink" title="图片 (Images)"></a>图片 (Images)</h3><p>Markdown 图片语法类似于链接，只是在最前面添加一个 <strong>感叹号</strong><code>!</code>。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">![<span class="string">图片描述</span>](<span class="link">image.jpg</span>)</span><br></pre></td></tr></table></figure>
<p>还可以在 URL 之后的圆括号内添加 <strong>图片标题</strong> ：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">![<span class="string">图片描述</span>](<span class="link">image.jpg &quot;图片标题&quot;</span>)</span><br></pre></td></tr></table></figure>
<p><strong>链接图片</strong>: 将图片 Markdown 语法用方括号括起来，然后在后面用圆括号括住链接地址。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">![alt text</span>](<span class="link">image.jpg</span>)](<span class="link">https://www.example.com</span>)\`</span><br></pre></td></tr></table></figure>
<p>引用式图片：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">![<span class="string">图片描述</span>][<span class="symbol">图片标签</span>]</span><br><span class="line"></span><br><span class="line">[<span class="symbol">图片标签</span>]: <span class="link">image.jpg &quot;图片标题&quot;</span></span><br></pre></td></tr></table></figure>
<h3 id="转义字符-Escaping-Characters"><a href="#转义字符-Escaping-Characters" class="headerlink" title="转义字符 (Escaping Characters)"></a>转义字符 (Escaping Characters)</h3><p>Markdown 语法使用 <strong>反斜杠</strong> <code>\</code> 来转义 Markdown 中的特殊字符。 例如： <code>\*</code> 将显示为 * 而不是斜体。</p>
<p><strong>可以转义的字符包括</strong> ：</p>
<ul>
<li>反斜杠 <code>\</code></li>
<li>反引号 <code>` </code></li>
<li>星号 <code>*</code></li>
<li>下划线 <code>_</code></li>
<li>花括号 <code>&#123;&#125;</code></li>
<li>方括号 <code>[]</code></li>
<li>尖括号 <code>&lt;&gt;</code></li>
<li>圆括号 <code>()</code></li>
<li>井号 <code>#</code></li>
<li>加号 <code>+</code></li>
<li>减号 <code>-</code></li>
<li>句点 <code>.</code></li>
<li>感叹号 <code>!</code></li>
<li>竖线 <code>|</code></li>
</ul>
<h3 id="HTML-元素"><a href="#HTML-元素" class="headerlink" title="HTML 元素"></a>HTML 元素</h3><p>可以在 Markdown 中直接使用 HTML 标签，当需要修改元素的属性时，使用 HTML 更方便，但并非所有 Markdown 应用都支持 HTML。</p>
<p><strong>最佳实践</strong> ：</p>
<ul>
<li>使用空行分隔块级 HTML 元素，例如 <code>&lt;div&gt;</code> 、 <code>&lt;table&gt;</code> 、 <code>&lt;pre&gt;</code> 和 <code>&lt;p&gt;</code> 。</li>
<li>不要用制表符或空格缩进 HTML 标签。</li>
<li>不能在块级 HTML 标签中使用 Markdown 语法</li>
</ul>
<h2 id="Markdown-扩展语法"><a href="#Markdown-扩展语法" class="headerlink" title="Markdown 扩展语法"></a>Markdown 扩展语法</h2><p>扩展语法是在基本语法的基础上添加的增强功能。并非所有的 Markdown 应用都支持这些扩展语法元素。</p>
<h3 id="删除线-Strikethrough"><a href="#删除线-Strikethrough" class="headerlink" title="删除线 (Strikethrough)"></a>删除线 (Strikethrough)</h3><p>使用 <strong>两个波浪号</strong> <code>~~</code> 包裹需要添加删除线的文本。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">~~这是删除线文本~~</span><br></pre></td></tr></table></figure>
<p><code>~~被删除的文字~~</code> 渲染为 <code>&lt;del&gt;被删除的文字&lt;/del&gt;</code> 。</p>
<h3 id="任务列表-Task-Lists"><a href="#任务列表-Task-Lists" class="headerlink" title="任务列表 (Task Lists)"></a>任务列表 (Task Lists)</h3><p>Markdown 语法使用 <strong>减号和方括号</strong> <code>- [ ]</code> 创建任务列表，使用 <code>- [x]</code> 来标记已完成的任务。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">-</span> [x] 已完成的任务</span><br><span class="line"><span class="bullet">-</span> [ ] 未完成的任务</span><br></pre></td></tr></table></figure>
<h3 id="表情符号-Emoji"><a href="#表情符号-Emoji" class="headerlink" title="表情符号 (Emoji)"></a>表情符号 (Emoji)</h3><p>可以直接复制粘贴 emoji 或者使用 <strong>emoji 简码</strong> 。例如： <code>:joy:</code> 会显示 😂</p>
<p><strong>注意</strong>: 表情符号简码在不同应用中可能不同。</p>
<h3 id="下标-Subscript"><a href="#下标-Subscript" class="headerlink" title="下标 (Subscript)"></a>下标 (Subscript)</h3><p>Markdown 语法使用 <strong>一个波浪号</strong> <code>~</code> 包裹下标文本。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">H~2~O</span><br></pre></td></tr></table></figure>
<p><code>H~2~O</code> 渲染为 <code>H&lt;sub&gt;2&lt;/sub&gt;O</code></p>
<p><strong>最佳实践</strong> ：可以在 HTML 中使用 <code>&lt;sub&gt;</code> 标签来实现下标。例如 <code>H&lt;sub&gt;2&lt;/sub&gt;O</code> 。</p>
<h3 id="上标-Superscript"><a href="#上标-Superscript" class="headerlink" title="上标 (Superscript)"></a>上标 (Superscript)</h3><p>使用 <strong>一个插入符号</strong> <code>^</code> 包裹上标文本。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">X^2^</span><br></pre></td></tr></table></figure>
<p><code>X^2^</code> 渲染为 <code>X&lt;sup&gt;2&lt;/sup&gt;</code></p>
<p><strong>最佳实践</strong> ：可以在 HTML 中使用 <code>&lt;sup&gt;</code> 标签来实现上标。例如 <code>X&lt;sup&gt;2&lt;/sup&gt;</code> 。</p>
<h3 id="自动-URL-链接-Automatic-URL-Linking"><a href="#自动-URL-链接-Automatic-URL-Linking" class="headerlink" title="自动 URL 链接 (Automatic URL Linking)"></a>自动 URL 链接 (Automatic URL Linking)</h3><p>许多 Markdown 处理器会自动将 URL 转换为链接。 例如：<a href="http://www.example.com">http://www.example.com</a> 会被自动转换为链接。</p>
<p>使用反引号 <code>` </code> 将 URL 括起来，以禁用自动链接： <code>`http://www.example.com` </code></p>
<h3 id="表格-Tables"><a href="#表格-Tables" class="headerlink" title="表格 (Tables)"></a>表格 (Tables)</h3><p>Markdown 语法使用 <strong>三个或多个减号 (—)</strong> 创建表头，并使用 <strong>竖线 (|)</strong> 分隔列。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">| 表头 1 | 表头 2 |</span><br><span class="line">| ------ | ------ |</span><br><span class="line">| 内容 1 | 内容 2 |</span><br></pre></td></tr></table></figure>
<p><strong>Markdown 表格语法最佳实践:</strong></p>
<ul>
<li>为了兼容性，在每一行的开头和结尾添加竖线。</li>
<li>表格单元格的宽度可以不同，渲染结果保持一致。</li>
<li>表格中不能使用标题、引用、列表、水平线、图像或大多数 HTML 标签。</li>
<li>可以在表格中使用 HTML <code>&lt;br&gt;</code> 标签来实现单元格内的换行和使用 <code>&lt;ul&gt;</code> <code>&lt;li&gt;</code> 等标签来创建列表。</li>
<li><strong>转义表格中的竖线</strong>: 使用 <code>&amp;#124;</code>来在表格中显示竖线。</li>
</ul>
<h3 id="脚注-Footnotes"><a href="#脚注-Footnotes" class="headerlink" title="脚注 (Footnotes)"></a>脚注 (Footnotes)</h3><p>使用 <code>[^数字]</code> 来创建脚注标记，并在文档的其他地方使用 <code>[^数字]:</code> 来定义脚注内容。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">这是一个带有脚注的句子。[^1]</span><br><span class="line"></span><br><span class="line">[<span class="symbol">^1</span>]: <span class="link">这是脚注的内容。</span></span><br></pre></td></tr></table></figure>
<p>脚注会在输出中按顺序编号。</p>
<p><strong>注意</strong>: 标识符不能包含空格或制表符。</p>
<h3 id="标题-ID-Heading-IDs"><a href="#标题-ID-Heading-IDs" class="headerlink" title="标题 ID (Heading IDs)"></a>标题 ID (Heading IDs)</h3><p>Markdown 可以使用 <code>&#123; #custom-id &#125;</code> 为标题添加自定义 ID，方便链接到文档内的特定部分。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section">## My Great Heading &#123;#custom-id&#125;</span></span><br></pre></td></tr></table></figure>
<p><strong>链接到标题 ID</strong>:</p>
<ul>
<li>可以使用 <code>#</code> 符号后跟自定义 ID 来创建链接。例如:  <figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">点击这个链接文本跳转到#custom-id 的位置</span>](<span class="link">#custom-id</span>)\`</span><br></pre></td></tr></table></figure></li>
<li>其他网站可以通过在网页 URL 后添加自定义 ID 来链接到该标题。例如:  <figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">链接文本</span>](<span class="link">https://www.example.com/extended-syntax#custom-id</span>)</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="定义列表-Definition-Lists"><a href="#定义列表-Definition-Lists" class="headerlink" title="定义列表 (Definition Lists)"></a>定义列表 (Definition Lists)</h3><p>可以使用 <code>term : definition</code> 来创建定义列表。在第一行键入术语，在下一行键入冒号加空格，然后是定义。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">术语</span><br><span class="line">: 定义 1</span><br><span class="line">: 定义 2</span><br></pre></td></tr></table></figure>
<h3 id="高亮-Highlights"><a href="#高亮-Highlights" class="headerlink" title="高亮 (Highlights)"></a>高亮 (Highlights)</h3><p>使用 <code>==</code> 包裹需要高亮的文本。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">==需要高亮的文字==</span><br></pre></td></tr></table></figure>
<p>也可以使用 <code>&lt;mark&gt;</code> 标签: <code>&lt;mark&gt;高亮文本&lt;/mark&gt;</code></p>
<h2 id="Markdown-技巧"><a href="#Markdown-技巧" class="headerlink" title="Markdown 技巧"></a>Markdown 技巧</h2><p>以下是一些在 Markdown 中进行高级操作的技巧，这些技巧并非所有应用都支持。</p>
<h3 id="下划线-Underline"><a href="#下划线-Underline" class="headerlink" title="下划线 (Underline)"></a>下划线 (Underline)</h3><p>Markdown 本身不支持下划线，可以使用 <code>&lt;ins&gt;</code> HTML 标签来实现。</p>
<p><code>&lt;ins&gt;下划线文字&lt;/ins&gt;</code> 渲染为 下划线文字</p>
<h3 id="缩进-Indent"><a href="#缩进-Indent" class="headerlink" title="缩进 (Indent)"></a>缩进 (Indent)</h3><p>Markdown 对缩进有特殊含义，通常使用空格键或制表符缩进段落是不起作用的，但是可以使用以下方式实现:</p>
<ul>
<li>使用 Markdown 编辑器支持缩进的功能。</li>
<li>使用 <code>&amp;nbsp;</code> HTML 实体来创建缩进。</li>
<li>例如： <code>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 这是缩进的段落。</code></li>
</ul>
<h3 id="居中-Center"><a href="#居中-Center" class="headerlink" title="居中 (Center)"></a>居中 (Center)</h3><p>Markdown 本身没有居中对齐的概念，可以使用 <code>&lt;center&gt;</code> HTML 标签来实现。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">center</span>&gt;</span>居中文本<span class="tag">&lt;/<span class="name">center</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>也可以可以使用 CSS <code>text-align:center</code> 属性来实现居中， 例如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">style</span>=<span class="string">&quot;text-align:center&quot;</span>&gt;</span>居中文本<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="颜色-Color"><a href="#颜色-Color" class="headerlink" title="颜色 (Color)"></a>颜色 (Color)</h3><p>Markdown 本身没有改变文本颜色的功能，可以使用 <code>&lt;font&gt;</code> HTML 标签来实现。</p>
<p><code>&lt;font color=&quot;red&quot;&gt;红色文字&lt;/font&gt;</code> 渲染为 红色文字</p>
<p>可以使用 CSS <code>color:blue</code> 属性来改变颜色, 例如:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">style</span>=<span class="string">&quot;color:blue&quot;</span>&gt;</span>蓝色文本<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>使用 <code>[注释]: #</code> 这种格式添加隐藏注释。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">这是可见的段落。</span><br><span class="line">[<span class="symbol">这是一个注释</span>]: <span class="link">#</span></span><br><span class="line">这是另一个可见的段落。</span><br></pre></td></tr></table></figure>
<p><strong>最佳实践:</strong> 在注释前后添加空行。</p>
<h3 id="提示框-Admonitions"><a href="#提示框-Admonitions" class="headerlink" title="提示框 (Admonitions)"></a>提示框 (Admonitions)</h3><p>可以使用引用和表情符号以及加粗文本来模拟提示框。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="quote">&gt; :warning: <span class="strong">**警告:**</span> 这是警告提示。</span></span><br><span class="line"><span class="quote">&gt; :memo: <span class="strong">**注意:**</span> 这是注意提示。</span></span><br><span class="line"><span class="quote">&gt; :bulb: <span class="strong">**技巧:**</span> 这是技巧提示。</span></span><br></pre></td></tr></table></figure>
<h3 id="图片大小-Image-Size"><a href="#图片大小-Image-Size" class="headerlink" title="图片大小 (Image Size)"></a>图片大小 (Image Size)</h3><p>使用 <code>&lt;img&gt;</code> HTML 标签的 <code>width</code> 和 <code>height</code> 属性来调整图片大小。例如：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;image.png&quot;</span> <span class="attr">width</span>=<span class="string">&quot;200&quot;</span> <span class="attr">height</span>=<span class="string">&quot;100&quot;</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<h3 id="图片标题-Image-Captions"><a href="#图片标题-Image-Captions" class="headerlink" title="图片标题 (Image Captions)"></a>图片标题 (Image Captions)</h3><p>可以使用 <code>&lt;figure&gt;</code> 和 <code>&lt;figcaption&gt;</code> HTML 标签来为图片添加标题。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">figure</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;image.jpg&quot;</span> <span class="attr">alt</span>=<span class="string">&quot;图片描述&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">figcaption</span>&gt;</span>这是图片标题<span class="tag">&lt;/<span class="name">figcaption</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">figure</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>也可以直接在图片下方添加文本，使用斜体强调，作为图片标题。</p>
<h3 id="链接目标-Link-Targets"><a href="#链接目标-Link-Targets" class="headerlink" title="链接目标 (Link Targets)"></a>链接目标 (Link Targets)</h3><p>可以使用 <code>&lt;a&gt;</code> HTML 标签的 <code>target=&quot;_blank&quot;</code> 属性来让链接在新标签页中打开。</p>
<p>例如:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;https://www.example.com&quot;</span> <span class="attr">target</span>=<span class="string">&quot;_blank&quot;</span>&gt;</span>在新标签页打开链接<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="符号-Symbols"><a href="#符号-Symbols" class="headerlink" title="符号 (Symbols)"></a>符号 (Symbols)</h3><p>直接复制粘贴符号到 Markdown 文档中，也可以使用 HTML 实体代码来插入符号。</p>
<p>例如: <code>&amp;copy;</code> 渲染为 ©</p>
<p>常见 HTML 实体代码:</p>
<ul>
<li><code>&amp;copy;</code> (版权符号 ©)</li>
<li><code>&amp;reg;</code> (注册商标符号 ®)</li>
<li><code>&amp;trade;</code> (商标符号 ™)</li>
<li><code>&amp;euro;</code> (欧元符号 €)</li>
<li><code>&amp;larr;</code> (左箭头 ←)</li>
<li><code>&amp;uarr;</code> (上箭头 ↑)</li>
<li><code>&amp;rarr;</code> (右箭头 →)</li>
<li><code>&amp;darr;</code> (下箭头 ↓)</li>
<li><code>&amp;#176;</code> (度 °)</li>
<li><code>&amp;#960;</code> (圆周率 π)</li>
</ul>
<h3 id="表格格式化-Table-Formats"><a href="#表格格式化-Table-Formats" class="headerlink" title="表格格式化 (Table Formats)"></a>表格格式化 (Table Formats)</h3><p><strong>表格内换行</strong>: 使用 <code>&lt;br&gt;</code> 标签来分隔表格内的段落。</p>
<p><strong>表格内列表</strong>: 使用 HTML 标签 ( <code>&lt;ul&gt;</code>, <code>&lt;li&gt;</code> )来在表格中添加列表。</p>
<p>有些 Markdown 应用可以自动生成目录，但是如果你的应用不支持此功能，可以使用列表和链接手动创建目录。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">-</span> [<span class="string">链接文本 1</span>](<span class="link">#heading-id-1</span>)</span><br><span class="line"><span class="bullet">-</span> [<span class="string">链接文本 2</span>](<span class="link">#heading-id-2</span>)</span><br><span class="line"><span class="bullet">-</span> [<span class="string">链接文本 3</span>](<span class="link">#heading-id-3</span>)</span><br></pre></td></tr></table></figure>
<p>需要在文章中使用对应的标题 ID。</p>
<h3 id="视频-Videos"><a href="#视频-Videos" class="headerlink" title="视频 (Videos)"></a>视频 (Videos)</h3><p>如果应用支持 HTML， 可以直接嵌入视频网站提供的 HTML 代码。如果应用不支持，可以使用图片和链接来模拟视频嵌入。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">[<span class="string">![视频标题</span>](<span class="link">视频封面图.jpg</span>)](<span class="link">视频链接</span>)</span><br></pre></td></tr></table></figure>
<h2 id="CommonMark-规范"><a href="#CommonMark-规范" class="headerlink" title="CommonMark 规范"></a>CommonMark 规范</h2><p>CommonMark 是一份对 Markdown 语法的标准化规范。以下是一些重要概念：</p>
<ul>
<li><strong>字符和行</strong> ：任何字符序列都是有效的 CommonMark 文档。字符是 Unicode 编码点。</li>
<li><strong>空格字符</strong> ：包括 Unicode Zs 类别的字符、制表符（U+0009）、换行符（U+000A）、换页符（U+000C）和回车符（U+000D）。</li>
<li><strong>制表符</strong> ：制表符不展开为空格，但在需要空格定义块结构时，制表符的表现如同被替换为制表符停止位为 4 个字符的空格。</li>
<li><strong>不安全字符</strong> ：Unicode 字符 U+0000 必须替换为替换字符（U+FFFD）。</li>
<li><strong>反斜杠转义</strong> ：任何 ASCII 标点字符都可以被反斜杠转义。反斜杠后的其他字符被视为字面反斜杠，转义字符会被视为常规字符，失去 Markdown 的特殊含义。</li>
<li><strong>实体引用</strong> ：实体引用由 <code>&amp;</code> + HTML5 实体名称 + <code>;</code> 组成。例如 <code>&amp;amp;</code> 代表 <code>&amp;</code> ， <code>&amp;copy;</code> 代表 <code>©</code> 。</li>
<li><strong>数字字符引用</strong> ：十进制字符引用由 <code>&amp;#</code> + 1-7 位阿拉伯数字 + <code>;</code> 组成，例如： <code>&amp;#35;</code> 代表 <code>#</code> 。十六进制字符引用由 <code>&amp;#</code> + <code>x</code> 或 <code>X</code> + 1-6 位十六进制数字 + <code>;</code> 组成，例如 <code>&amp;#X22;</code> 代表 <code>&quot;</code> 。</li>
<li><strong>HTML 块</strong> ：HTML 块是指由 HTML 标签组成的块，其起始和结束条件由特定规则定义。</li>
<li><strong>容器块</strong> ：包括块引用和列表项。列表是列表项的元容器。</li>
<li><strong>列表项</strong> ：列表项的标记可以是项目符号或有序列表标记。项目符号列表标记使用 <code>-</code> ， <code>+</code> 或 <code>*</code> ；有序列表标记使用 1-9 位数字后跟 <code>.</code> 或 <code>)</code> 。</li>
<li><strong>内联元素</strong> ：Markdown 的内联元素按从左到右的顺序解析，包括代码跨度、强调、链接、图片等。</li>
<li><strong>代码跨度</strong> ：由反引号包裹，内容会进行规范化处理，例如删除首尾空格。</li>
<li><strong>强调和加粗</strong> ：使用 <code>*</code> 或 <code>_</code> 实现强调，使用 <code>**</code> 或 <code>__</code> 实现加粗，具体的规则较复杂。</li>
<li><strong>链接和图片</strong> ：链接使用 <code>[]</code> 和 <code>()</code> ，图片语法类似于链接，只是在前面添加一个 <code>!</code>。</li>
<li><strong>自动链接</strong> ：使用尖括号 <code>&lt;&gt;</code> 包裹 URL 会被自动解析为链接。</li>
<li><strong>原始 HTML</strong> ：在尖括号 <code>&lt;&gt;</code> 中看起来像 HTML 标签的文本会被解析为原始 HTML。</li>
</ul>
<p><strong>关键点</strong> ：</p>
<ul>
<li><strong>强调和加粗的规则</strong> ：需要注意左侧和右侧的定界符，以及 Unicode 标点符号的影响。</li>
<li><strong>代码跨度</strong> ：代码跨度的优先级高于除 HTML 标签和自动链接外的其他内联结构。</li>
<li><strong>链接</strong> ：注意链接文本和链接地址的语法，以及如何使用引用式链接。</li>
<li><strong>HTML 块</strong> ：了解 HTML 块的起始和结束条件，以及如何在 Markdown 中使用 HTML 标签。</li>
<li><strong>列表</strong> ：需要了解列表的类型，列表项的标记，以及列表的嵌套和懒惰延续行规则。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本教程手册涵盖了 Markdown 的基本语法、扩展语法和一些实用技巧。Markdown 的目标是简化文档编写，让你专注于内容而不是格式。通过掌握这些语法，你可以更高效地创建结构化的文档。希望本教程手册能帮助你更好地使用 Markdown。</p>
<p>请注意，某些特性可能因不同的 Markdown 解析器而有所不同。为了获得最佳效果，请查阅你正在使用的特定 Markdown 应用的文档。</p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>编辑器</tag>
        <tag>Typora</tag>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Github Pages 和 Hexo 搭建自己的独立博客</title>
    <url>/undefined/7a776471.html</url>
    <content><![CDATA[<p><h1>入门</h1></p>
<p><h2>Github</h2><br>搭建一个这样的 Blog 首先你得知道什么是 Github 和 Github Pages，你可以理解为咱们没有服务器，咱们将页面托管到了 Github 上</p>
<p><h2>Github Pages</h2><br>Github Pages 可以被认为是用户编写的、托管在 github 上的静态网页。使用 Github Pages 可以为你提供一个免费的服务器，免去了自己搭建服务器和写数据库的麻烦。此外还可以绑定自己的域名。</p>
<p><h2>Hexo</h2><br>Hexo 是一个快速、简洁且高效的博客框架。Hexo 使用 Markdown（或其他渲染引擎）解析文章，在几秒内，即可利用靓丽的主题生成静态网页。</p>
<p><h1>node.js</h1><br><details class="folding-tag" ><summary> 安装node.js </summary>
              <div class='content'>
              <p>进入 node.js 官网：<a href="https://nodejs.cn/download/">node.js官网</a><br>按照自己的机器选择对应的版本下载，我是 windous 64 位<br><img src="https://www.helloimg.com/i/2024/12/27/676eb7764e8b5.png" alt="nodejs"><br>然后差不多就是一直下一步 hhh~<br>如果遇到什么问题，一般百度都能解决，这里就不在赘述.</p>
              </div>
            </details></p>
<p><h1>Git</h1><br><details class="folding-tag" ><summary> 安装git </summary>
              <div class='content'>
              <p>这是 Git 官网：<a href="https://git-scm.com/downloads">git官网</a><br>一般就是下载好自己对应的版本后一直下一步就好了</p>
              </div>
            </details></p>
<p><h1>检验软件是否安装成功</h1><br>同时按下 Win 键和 R 键打开运行窗口,输入 <code>cmd</code> ，然后输入以下命令，有相应版本信息显示则安装成功，若不正确可以卸载软件重新安装。<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git --version</span><br><span class="line">node -v</span><br><span class="line">npm -v</span><br></pre></td></tr></table></figure><br>此外若安装成功，在桌面右键鼠标，可以看到菜单里多了 Git GUI Here 和 Git Bash Here两个选项，第一个是图形界面的Git操作，另一个是命令行，我们一般使用第二个<br><img src="https://www.helloimg.com/i/2024/12/27/676eb7668f7cc.png" alt="git" title="git"></p>
<p><h1>Hexo 安装</h1><br>选择一个磁盘，新建一个文件夹，自己重命名文件夹（如：我的文件夹为：F\QiQi_Blog），博客相关文件将储存在此文件夹下，在该文件夹下右键鼠标，点击 Git Bash Here，输入以下 npm 命令即可安装,第一次可能需要久一点输入命令后等一等，再输入第二条命令<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-cli -g</span><br></pre></td></tr></table></figure><br>这个命令表示安装 hexo 的脚手架，如图所示即为安装成功<br><img src="https://www.helloimg.com/i/2024/12/27/676eb76977098.png" alt="hexo" title="hexo"></p>
<p><h1>Hexo 初始化配置</h1><br>第一个命令，如果失败了就再输入一次，第二个命令表示安装 hexo 部署到 git page 的 deployer<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo init</span><br><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure><br>Hexo 安装完成后，将会在指定文件夹中新建所需要的文件</p>
<p><h1>本地查看效果</h1><br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo generate</span><br><span class="line">hexo server</span><br></pre></td></tr></table></figure><br>执行完即可登录 <a href="http://localhost:4000/">http://localhost:4000/</a> 查看效果</p>
<h2 id="安装主题"><a href="#安装主题" class="headerlink" title="安装主题"></a>安装主题</h2><p>推荐主题 butterfly <a href="https://butterfly.js.org/posts/21cfbf15/">安装及配置</a></p>
<p>请认真阅读配置文档</p>
<h2 id="托管到-GitHub"><a href="#托管到-GitHub" class="headerlink" title="托管到 GitHub"></a>托管到 GitHub</h2><p>安装 hexo-deployer-git</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
<p>修改 _config.yml 文件 deploy 部分</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repository:</span> <span class="string">git@github.com:&lt;用户名&gt;/&lt;用户名&gt;.github.io.git</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>
<p>修改好配置后记得 CTRL+S 保存，运行以下命令，将代码部署到 GitHub</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo cl; hexo g; hexo d</span><br></pre></td></tr></table></figure>
<p>如果出现<code>Deploy done</code>，则说明部署成功</p>
<h2 id="部署到-Vercel"><a href="#部署到-Vercel" class="headerlink" title="部署到 Vercel"></a>部署到 Vercel</h2><p><a href="https://vercel.com/">官网地址</a></p>
<p>使用 GitHub 注册并登录，选择 Add New &gt; Project</p>
<p>找到之前创建的库，点后面的 Import，直接点 Deploy 等待部署完成</p>
<p>之后到 Project 页面 &gt; Domains 添加自己的域名，在域名管理页解析即可通过域名访问了！</p>
<h2 id="📚-总结"><a href="#📚-总结" class="headerlink" title="📚 总结"></a>📚 总结</h2><p>完成以上步骤后，您的 Hexo 博客就搭建完成了！主要实现了:</p>
<ul>
<li>基础环境配置</li>
<li>Hexo 框架安装</li>
<li>GitHub Pages 部署</li>
<li>主题美化</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>Github</tag>
        <tag>博客</tag>
        <tag>Github Pages</tag>
      </tags>
  </entry>
  <entry>
    <title>最好用的Markdown编辑器-Typora</title>
    <url>/undefined/c6c552a3.html</url>
    <content><![CDATA[<h2 id="📝-前言"><a href="#📝-前言" class="headerlink" title="📝 前言"></a>📝 前言</h2><p>接触前端的人一定知道 Markdown，这是一种轻量级标记语言，被广泛用于撰写帮助文档、论坛消息和博客文章。Typora 让 Markdown 写作变得简单优雅。</p>
<blockquote>
<p>[!NOTE]<br>Markdown 已成为 GitHub、SourceForge 等平台的标准写作格式。</p>
</blockquote>
<hr>
<h2 id="为什么选择-Typora"><a href="#为什么选择-Typora" class="headerlink" title="为什么选择 Typora"></a>为什么选择 Typora</h2><h3 id="1-简单易用"><a href="#1-简单易用" class="headerlink" title="1. 简单易用"></a>1. 简单易用</h3><p>特点：</p>
<ul>
<li>所见即所写(WYSIWYG)编辑模式</li>
<li>清爽的界面设计</li>
<li>无需记忆复杂语法</li>
</ul>
<h3 id="2-强大的功能"><a href="#2-强大的功能" class="headerlink" title="2. 强大的功能"></a>2. 强大的功能</h3><p>以表格为例，无需手动编写复杂的语法：</p>
<p>支持的功能：</p>
<ul>
<li>表格编辑</li>
<li>图片插入</li>
<li>数学公式</li>
<li>代码高亮</li>
<li>目录生成</li>
<li>多种导出格式</li>
</ul>
<hr>
<h2 id="💡-如何用-Typora-写-Hexo-文章"><a href="#💡-如何用-Typora-写-Hexo-文章" class="headerlink" title="💡 如何用 Typora 写 Hexo 文章"></a>💡 如何用 Typora 写 Hexo 文章</h2><h3 id="1-创建文章"><a href="#1-创建文章" class="headerlink" title="1. 创建文章"></a>1. 创建文章</h3><p>bash</p>
<div class="table-container">
<table>
<thead>
<tr>
<th><code># 在博客根目录运行 hexo new &lt;文章标题&gt;</code></th>
</tr>
</thead>
<tbody>
<tr>
<td></td>
</tr>
</tbody>
</table>
</div>
<h3 id="2-编辑文章"><a href="#2-编辑文章" class="headerlink" title="2. 编辑文章"></a>2. 编辑文章</h3><ol>
<li>打开 Typora</li>
<li>编辑 front-matter</li>
<li>撰写文章内容</li>
<li>复制到 Hexo 文章中</li>
</ol>
<blockquote>
<p>[!TIP]<br>关于 front-matter 的详细说明，可以参考<a href="https://www.xmdblog.com/posts/2ebce51e.html">这篇文章</a></p>
</blockquote>
<hr>
<h2 id="关于购买"><a href="#关于购买" class="headerlink" title="关于购买"></a>关于购买</h2><h3 id="官方购买"><a href="#官方购买" class="headerlink" title="官方购买"></a>官方购买</h3><ul>
<li>官网：<a href="https://typoraio.cn/">Typora 官方中文站</a></li>
<li>价格：80 RMB (荔枝软件商店)</li>
</ul>
<h3 id="免费替代方案"><a href="#免费替代方案" class="headerlink" title="免费替代方案"></a>免费替代方案</h3><p>如果预算有限，可以考虑：</p>
<ul>
<li><a href="https://www.marktext.cc/">MarkText</a>：开源免费</li>
<li><a href="https://code.visualstudio.com/">VSCode + Markdown 插件</a>：功能丰富</li>
</ul>
<blockquote>
<p>[!NOTE]<br>个人建议支持正版 Typora，物有所值。</p>
</blockquote>
<hr>
<h2 id="📚-总结"><a href="#📚-总结" class="headerlink" title="📚 总结"></a>📚 总结</h2><p>Typora 是一款优秀的 Markdown 编辑器：</p>
<ul>
<li>界面简洁优雅</li>
<li>功能强大实用</li>
<li>适合写作分享</li>
<li>值得投资购买</li>
</ul>
]]></content>
      <categories>
        <category>分享</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>编辑器</tag>
        <tag>Typora</tag>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>VSCode报错此系统上禁止运行脚本</title>
    <url>/undefined/8d48c6a1.html</url>
    <content><![CDATA[<h2 id="📝-问题描述"><a href="#📝-问题描述" class="headerlink" title="📝 问题描述"></a>📝 问题描述</h2><p>在使用 VSCode 时可能会遇到”此系统上禁止运行脚本”的错误提示，这是由于 Windows 默认的 PowerShell 执行策略限制导致的。</p>
<h2 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h2><p>可以通过 PowerShell 命令修改执行策略:<br> powershell<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Set-ExecutionPolicy RemoteSigned</span><br></pre></td></tr></table></figure></p>
<h3 id="验证设置"><a href="#验证设置" class="headerlink" title="验证设置"></a>验证设置</h3><p>设置完成后，重启 VSCode 验证问题是否解决。</p>
<hr>
<h2 id="📚-总结"><a href="#📚-总结" class="headerlink" title="📚 总结"></a>📚 总结</h2><p>通过修改 Windows PowerShell 的执行策略，我们解决了 VSCode 无法运行脚本的问题。这个设置不会影响系统安全性，但可以让开发工作更顺畅。</p>
]]></content>
      <categories>
        <category>分享</category>
      </categories>
      <tags>
        <tag>vscode</tag>
      </tags>
  </entry>
</search>
